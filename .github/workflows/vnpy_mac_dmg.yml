# VNPY 纯Mac版dmg GitHub Actions自动打包
name: VNPY Mac DMG Auto Build

# 触发规则：1.推送到main分支自动打包 2.手动触发（推荐）
on:
  push:
    branches: [ main ]  # 【需修改】可改为你的开发分支，如dev
  workflow_dispatch:    # 手动触发开关，保留！

# 仅执行Mac打包作业
jobs:
  build-mac-dmg:
    runs-on: macos-14  # 使用macos-14，资源更充足，排队时间更短
    steps:
      # 步骤1：拉取仓库中的VNPY优化源码
      - name: Checkout Source Code
        uses: actions/checkout@v4

      # 步骤2：配置Python3.10环境（VNPY新版本要求>=3.10）
      - name: Set Up Python 3.10
        uses: actions/setup-python@v5
        with:
          python-version: '3.10'
          cache: 'pip'  # 缓存依赖，大幅加速后续打包

      # 步骤3：安装所有依赖（VNPY+打包工具+底层依赖）
      - name: Install All Dependencies
        run: |
          python -m pip install --upgrade pip setuptools wheel
          
          # 安装VNPY（使用开发模式，包含所有修改）
          pip install -e .
          
          # 安装打包工具
          pip install pyinstaller
          
          # 安装图标处理工具（Pillow用于图标转换）
          pip install Pillow || echo "Pillow安装失败，将尝试其他方法..."
          
          # 安装create-dmg（Mac DMG制作工具）
          brew install create-dmg || echo "create-dmg安装失败，尝试继续..."
          
          # 安装TA-Lib（VNPY必备）
          brew install ta-lib || echo "ta-lib可能已安装或需要手动处理"
          
          # 安装可选依赖（用于Alpha策略等）
          pip install polars pandas numpy matplotlib || echo "可选依赖安装失败，继续..."
          
          # 验证关键依赖
          python -c "import vnpy; import loguru; import tzlocal; print('✅ 核心依赖验证通过')"
          python -c "import polars; print('✅ polars已安装')" || echo "⚠️  polars未安装（Alpha策略需要）"
          python -c "from PIL import Image; print('✅ Pillow已安装（图标处理）')" || echo "⚠️  Pillow未安装（图标转换可能失败）"

      # 步骤4：PyInstaller打包为Mac可执行文件
      - name: Package VNPY via PyInstaller
        run: |
          # 使用--onedir模式（目录模式），更稳定，Mac推荐
          # 注意：不使用--windowed，保留控制台输出以便调试
          # 如果确实需要无控制台，可以在Info.plist中设置LSUIElement
          pyinstaller \
          --onedir \
          --name VNPY-Optimized \
          --add-data "vnpy:vnpy" \
          --add-data "vnpy/trader/ui/ico:vnpy/trader/ui/ico" \
          --hidden-import vnpy \
          --hidden-import vnpy.trader \
          --hidden-import vnpy.trader.platform_utils \
          --hidden-import vnpy.trader.multiprocess_manager \
          --hidden-import vnpy.trader.enhanced_cta_template \
          --hidden-import vnpy.trader.history_manager \
          --hidden-import vnpy.trader.data_filter \
          --hidden-import vnpy.trader.optimization_metrics \
          --hidden-import vnpy.trader.enhanced_risk_manager \
          --hidden-import vnpy.trader.status_monitor \
          --hidden-import vnpy.trader.gateway_mac_adapter \
          --hidden-import vnpy.trader.multiprocess_backtester \
          --hidden-import vnpy.trader.optimization_visualization \
          --hidden-import vnpy.alpha \
          --hidden-import vnpy.alpha.strategy \
          --hidden-import vnpy.alpha.strategy.template \
          --hidden-import vnpy.alpha.strategy.strategies \
          --hidden-import vnpy.event \
          --hidden-import vnpy.trader.engine \
          --hidden-import vnpy.trader.ui \
          --hidden-import vnpy.trader.ui.mainwindow \
          --hidden-import vnpy.trader.ui.widget \
          --hidden-import loguru \
          --hidden-import tzlocal \
          --hidden-import talib \
          --hidden-import PySide6 \
          --hidden-import PySide6.QtCore \
          --hidden-import PySide6.QtGui \
          --hidden-import PySide6.QtWidgets \
          --hidden-import qdarkstyle \
          --collect-all PySide6 \
          --collect-all qdarkstyle \
          --collect-all vnpy \
          "vnpy_launcher.py"
          
          # 验证打包结果
          if [ -d "dist/VNPY-Optimized" ]; then
            echo "✅ PyInstaller打包成功"
            echo "打包目录内容："
            ls -lh dist/VNPY-Optimized/
            echo ""
            echo "查找可执行文件："
            find dist/VNPY-Optimized -type f -perm +111 -o -name "VNPY-Optimized" | head -10
            echo ""
            # 检查可执行文件
            if [ -f "dist/VNPY-Optimized/VNPY-Optimized" ]; then
              echo "✅ 找到可执行文件: dist/VNPY-Optimized/VNPY-Optimized"
              chmod +x dist/VNPY-Optimized/VNPY-Optimized
            else
              echo "⚠️  可执行文件不在预期位置，查找中..."
              EXEC=$(find dist/VNPY-Optimized -type f -perm +111 | head -1)
              if [ -n "$EXEC" ]; then
                echo "找到可执行文件: $EXEC"
              else
                echo "❌ 未找到可执行文件"
                exit 1
              fi
            fi
          else
            echo "❌ PyInstaller打包失败"
            exit 1
          fi

      # 步骤5：整理Mac标准.app目录结构（保证dmg兼容性）
      - name: Organize Mac App Structure
        run: |
          # 创建.app目录结构
          mkdir -p VNPY.app/Contents/MacOS
          mkdir -p VNPY.app/Contents/Resources
          
          # 复制打包后的文件到.app中
          echo "复制打包文件到.app..."
          cp -r dist/VNPY-Optimized/* VNPY.app/Contents/MacOS/
          
          # 确保可执行文件存在（PyInstaller --onedir模式）
          if [ ! -f "VNPY.app/Contents/MacOS/VNPY-Optimized" ]; then
            echo "⚠️  可执行文件不在预期位置，查找中..."
            # 查找可执行文件
            EXECUTABLE=$(find dist/VNPY-Optimized -type f -perm +111 -o -name "VNPY-Optimized" | head -1)
            if [ -n "$EXECUTABLE" ]; then
              echo "找到可执行文件: $EXECUTABLE"
              # 复制到正确位置
              cp "$EXECUTABLE" VNPY.app/Contents/MacOS/VNPY-Optimized
              chmod +x VNPY.app/Contents/MacOS/VNPY-Optimized
              echo "✅ 可执行文件已复制到正确位置"
            else
              echo "❌ 未找到可执行文件，列出dist目录："
              ls -la dist/VNPY-Optimized/
              exit 1
            fi
          fi
          
          # 创建应用图标（从.ico转换为.icns）
          echo "创建应用图标..."
          if [ -f "vnpy/trader/ui/ico/vnpy.ico" ]; then
            # 创建临时图标目录
            mkdir -p icon_temp.iconset
            
            # 使用Python转换图标（使用-c参数避免heredoc问题）
            python3 -c "
import sys
from PIL import Image
import os

try:
    ico_path = 'vnpy/trader/ui/ico/vnpy.ico'
    if os.path.exists(ico_path):
        img = Image.open(ico_path)
        sizes = [
            (16, 16, 'icon_16x16.png'),
            (32, 32, 'icon_16x16@2x.png'),
            (32, 32, 'icon_32x32.png'),
            (64, 64, 'icon_32x32@2x.png'),
            (128, 128, 'icon_128x128.png'),
            (256, 256, 'icon_128x128@2x.png'),
            (256, 256, 'icon_256x256.png'),
            (512, 512, 'icon_256x256@2x.png'),
            (512, 512, 'icon_512x512.png'),
            (1024, 1024, 'icon_512x512@2x.png'),
        ]
        os.makedirs('icon_temp.iconset', exist_ok=True)
        for w, h, name in sizes:
            resized = img.resize((w, h), Image.Resampling.LANCZOS)
            resized.save(f'icon_temp.iconset/{name}', 'PNG')
        print('✅ 图标PNG文件已生成')
    else:
        print('⚠️  vnpy.ico不存在，跳过图标生成')
except Exception as e:
    print(f'⚠️  图标转换失败: {e}')
    import subprocess
    try:
        subprocess.run(['sips', '-s', 'format', 'png', '--out', 'icon_temp.iconset/icon_512x512.png', ico_path], check=True)
        print('✅ 使用sips转换图标')
    except:
        print('⚠️  无法转换图标，将使用默认图标')
" || echo "⚠️  Python图标转换失败，尝试使用sips..."
            
            # 如果Python转换失败，尝试使用sips直接转换
            if [ ! -d "icon_temp.iconset" ] || [ -z "$(ls -A icon_temp.iconset 2>/dev/null)" ]; then
              echo "尝试使用sips转换图标..."
              sips -s format png vnpy/trader/ui/ico/vnpy.ico --out icon_temp.iconset/icon_512x512.png 2>/dev/null || echo "⚠️  sips转换也失败"
            fi
            
            # 使用iconutil生成.icns文件
            if [ -d "icon_temp.iconset" ] && [ "$(ls -A icon_temp.iconset)" ]; then
              iconutil -c icns icon_temp.iconset -o VNPY.app/Contents/Resources/vnpy.icns 2>/dev/null || {
                echo "⚠️  iconutil失败，尝试其他方法..."
                # 如果iconutil失败，至少复制一个PNG作为备用
                if [ -f "icon_temp.iconset/icon_512x512.png" ]; then
                  cp icon_temp.iconset/icon_512x512.png VNPY.app/Contents/Resources/vnpy.png
                fi
              }
              rm -rf icon_temp.iconset
              echo "✅ 应用图标已创建"
            else
              echo "⚠️  图标目录为空，跳过图标创建"
            fi
          else
            echo "⚠️  vnpy.ico不存在，跳过图标创建"
          fi
          
          # 复制UI图标资源到MacOS目录（确保UI图标可用）
          if [ -d "vnpy/trader/ui/ico" ]; then
            echo "复制UI图标资源..."
            mkdir -p VNPY.app/Contents/MacOS/vnpy/trader/ui/ico
            cp -r vnpy/trader/ui/ico/* VNPY.app/Contents/MacOS/vnpy/trader/ui/ico/ 2>/dev/null || true
            echo "✅ UI图标资源已复制"
          fi
          
          # 验证图标资源
          if [ -d "VNPY.app/Contents/MacOS/vnpy/trader/ui/ico" ]; then
            ICON_COUNT=$(find VNPY.app/Contents/MacOS/vnpy/trader/ui/ico -name "*.ico" | wc -l)
            echo "✅ 图标资源验证: 找到 $ICON_COUNT 个图标文件"
          fi
          
          # 验证应用图标
          if [ -f "VNPY.app/Contents/Resources/vnpy.icns" ]; then
            echo "✅ 应用图标文件已创建: vnpy.icns"
          elif [ -f "VNPY.app/Contents/Resources/vnpy.png" ]; then
            echo "✅ 应用图标文件已创建: vnpy.png（备用）"
          else
            echo "⚠️  应用图标文件未创建，将使用默认图标"
          fi
          
          # 创建Info.plist（Mac App必需）
          # 检查图标文件是否存在
          if [ -f "VNPY.app/Contents/Resources/vnpy.icns" ]; then
            ICON_KEY='    <key>CFBundleIconFile</key>
    <string>vnpy.icns</string>'
          elif [ -f "VNPY.app/Contents/Resources/vnpy.png" ]; then
            ICON_KEY='    <key>CFBundleIconFile</key>
    <string>vnpy.png</string>'
          else
            ICON_KEY=''
          fi
          
          printf '%s\n' \
            '<?xml version="1.0" encoding="UTF-8"?>' \
            '<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">' \
            '<plist version="1.0">' \
            '<dict>' \
            '    <key>CFBundleExecutable</key>' \
            '    <string>VNPY-Optimized</string>' \
            '    <key>CFBundleIdentifier</key>' \
            '    <string>com.vnpy.optimized</string>' \
            '    <key>CFBundleName</key>' \
            '    <string>VNPY优化版</string>' \
            '    <key>CFBundleVersion</key>' \
            '    <string>1.0</string>' \
            '    <key>CFBundleShortVersionString</key>' \
            '    <string>1.0</string>' \
            '    <key>CFBundlePackageType</key>' \
            '    <string>APPL</string>' \
            "$ICON_KEY" \
            '    <key>LSMinimumSystemVersion</key>' \
            '    <string>10.15</string>' \
            '    <key>NSHighResolutionCapable</key>' \
            '    <true/>' \
            '    <key>LSUIElement</key>' \
            '    <false/>' \
            '    <key>LSEnvironment</key>' \
            '    <dict>' \
            '        <key>PYTHONUNBUFFERED</key>' \
            '        <string>1</string>' \
            '    </dict>' \
            '</dict>' \
            '</plist>' > VNPY.app/Contents/Info.plist
          
          # 最终验证.app结构
          echo "验证.app结构..."
          if [ -f "VNPY.app/Contents/MacOS/VNPY-Optimized" ]; then
            echo "✅ .app结构创建成功"
            echo "可执行文件权限："
            ls -l VNPY.app/Contents/MacOS/VNPY-Optimized
            echo ""
            echo "测试可执行文件："
            VNPY.app/Contents/MacOS/VNPY-Optimized --version 2>&1 | head -5 || echo "（无法获取版本信息，但文件存在）"
            chmod +x VNPY.app/Contents/MacOS/VNPY-Optimized
            file VNPY.app/Contents/MacOS/VNPY-Optimized
            ls -lh VNPY.app/Contents/MacOS/VNPY-Optimized
          else
            echo "❌ .app结构创建失败"
            echo "VNPY.app/Contents/MacOS/ 目录内容："
            ls -la VNPY.app/Contents/MacOS/ || true
            echo ""
            echo "dist/VNPY-Optimized/ 目录内容："
            ls -la dist/VNPY-Optimized/ || true
            exit 1
          fi
          
          # 验证Info.plist
          if [ -f "VNPY.app/Contents/Info.plist" ]; then
            echo "✅ Info.plist 已创建"
          else
            echo "❌ Info.plist 未创建"
            exit 1
          fi

      # 步骤6：制作dmg镜像（Mac安装包，带标准安装界面）
      - name: Build DMG Image
        run: |
          # 创建DMG内容目录
          mkdir -p dmg-contents
          
          # 复制.app到DMG目录
          cp -R VNPY.app dmg-contents/
          
          # 创建Applications文件夹的快捷方式（拖拽目标）
          ln -s /Applications dmg-contents/Applications
          
          # 创建README文件（安装说明）
          printf '%s\n' \
            '============================================' \
            'VNPY Mac优化版 - 安装说明' \
            '============================================' \
            '' \
            '📦 安装步骤：' \
            '1. 将 VNPY.app 拖拽到 Applications 文件夹' \
            '2. 在终端执行以下命令（解决安全提示）：' \
            '   xattr -d com.apple.quarantine /Applications/VNPY.app' \
            '3. 现在可以正常双击打开 VNPY优化版了！' \
            '' \
            '🔒 安全提示处理（三种方法任选其一）：' \
            '方法1（推荐）：在终端执行：' \
            '   xattr -d com.apple.quarantine /Applications/VNPY.app' \
            '' \
            '方法2：右键点击 VNPY.app -> 选择"打开"' \
            '' \
            '方法3：系统设置 -> 隐私与安全性 -> 点击"仍要打开"' \
            '' \
            '💻 系统要求：' \
            '- macOS 10.15 (Catalina) 或更高版本' \
            '- Apple Silicon (M1/M2/M3) 或 Intel 处理器' \
            '' \
            '✨ 功能特性：' \
            '- Mac系统完美适配' \
            '- A股量化交易支持' \
            '- 增强策略模板' \
            '- 多进程策略执行' \
            '- 历史数据管理' \
            '- 优化指标计算' \
            '' \
            '📖 更多帮助：' \
            '- 查看 Mac安全验证处理指南.md 了解详细说明' \
            '- 如有问题，请查看使用说明文档' \
            '' \
            '============================================' > dmg-contents/README.txt
          
          # 检查create-dmg是否可用
          if ! command -v create-dmg &> /dev/null; then
            echo "⚠️  create-dmg未安装，使用hdiutil创建DMG（带安装界面）"
            
            # 使用hdiutil创建临时DMG
            hdiutil create -volname "VNPY优化版-Mac" \
              -srcfolder dmg-contents \
              -ov -format UDRW \
              "VNPY-Mac-Optimized-temp.dmg"
            
            # 挂载临时DMG
            hdiutil attach "VNPY-Mac-Optimized-temp.dmg" -mountpoint /Volumes/VNPY优化版-Mac
            
            # 设置DMG窗口属性（使用AppleScript）
            osascript -e 'tell application "Finder"' \
              -e '  tell disk "VNPY优化版-Mac"' \
              -e '    open' \
              -e '    set current view of container window to icon view' \
              -e '    set toolbar visible of container window to false' \
              -e '    set statusbar visible of container window to false' \
              -e '    set the bounds of container window to {200, 120, 1000, 720}' \
              -e '    set viewOptions to the icon view options of container window' \
              -e '    set arrangement of viewOptions to not arranged' \
              -e '    set icon size of viewOptions to 100' \
              -e '    set position of item "VNPY.app" of container window to {200, 300}' \
              -e '    set position of item "Applications" of container window to {600, 300}' \
              -e '    set position of item "README.txt" of container window to {400, 500}' \
              -e '    close' \
              -e '    open' \
              -e '    update without registering applications' \
              -e '    delay 2' \
              -e '  end tell' \
              -e 'end tell'
            
            # 卸载
            hdiutil detach /Volumes/VNPY优化版-Mac
            
            # 转换为压缩格式
            hdiutil convert "VNPY-Mac-Optimized-temp.dmg" \
              -format UDZO \
              -o "VNPY-Mac-Optimized.dmg"
            
            # 清理临时文件
            rm -f "VNPY-Mac-Optimized-temp.dmg"
          else
            # 使用create-dmg创建更美观的DMG
            create-dmg \
              --volname "VNPY优化版-Mac" \
              --window-pos 200 120 \
              --window-size 800 600 \
              --icon-size 100 \
              --icon "VNPY.app" 200 300 \
              --hide-extension "VNPY.app" \
              --app-drop-link 600 300 \
              --hdiutil-quiet \
              "VNPY-Mac-Optimized.dmg" \
              dmg-contents/
          fi
          
          # 验证DMG文件
          if [ -f "VNPY-Mac-Optimized.dmg" ]; then
            echo "✅ DMG创建成功"
            ls -lh VNPY-Mac-Optimized.dmg
            echo "DMG内容："
            hdiutil attach VNPY-Mac-Optimized.dmg -mountpoint /Volumes/VNPY-TEST -quiet 2>/dev/null || true
            ls -la /Volumes/VNPY-TEST/ 2>/dev/null || true
            hdiutil detach /Volumes/VNPY-TEST -quiet 2>/dev/null || true
          else
            echo "❌ DMG创建失败"
            exit 1
          fi

      # 步骤7：上传dmg产物（可在GitHub直接下载）
      - name: Upload DMG Artifact
        uses: actions/upload-artifact@v4
        with:
          name: VNPY-Mac-DMG  # 产物在Action页面的显示名称
          path: ./VNPY-Mac-Optimized.dmg  # 对应步骤6的dmg文件名
